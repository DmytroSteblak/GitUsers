import React from 'react';

import './header.scss';

const Header: React.FC = () => {
    return (
        <header  className="app__header">
            GitHub Searcher
        </header>
    );
};

export default Header;
